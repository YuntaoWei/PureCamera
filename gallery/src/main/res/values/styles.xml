<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AlbumActivity" parent="@style/AppTheme">
        <item name="android:homeAsUpIndicator">@drawable/ic_launcher_background</item>
    </style>

    <style name="PhotoPageActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorAccent">@android:color/holo_blue_light</item>
    </style>

    <style name="popup_window_animation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/anim_popup_window_in</item>
        <item name="android:windowExitAnimation">@anim/anim_popup_window_out</item>
    </style>

    <style name="ConfirmDialogStyle" parent="Theme.AppCompat.Light.Dialog.Alert">

    </style>

    <style name="draw_dialog" parent="@android:style/Theme.DeviceDefault.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/detail_dialog_background</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
